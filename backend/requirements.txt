# ============================================================
# SIGMA Backend — Dépendances Python
# ============================================================
# Installer avec : pip install -r requirements.txt
# ============================================================

# --- Framework Web ---
fastapi==0.136.3
uvicorn[standard]==0.48.0
starlette==1.2.0

# --- Validation & Sérialisation ---
pydantic==2.13.4
pydantic_core==2.46.4
pydantic-settings>=2.0.0          # BaseSettings (chargement .env)
email-validator>=2.0.0            # Validation EmailStr (Pydantic)
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# --- Base de données ---
SQLAlchemy==2.0.50
asyncpg==0.31.0                   # Driver PostgreSQL async
psycopg2-binary==2.9.12           # Driver PostgreSQL sync (migrations Alembic)
alembic==1.18.4                   # Migrations de schéma

# --- Redis ---
redis==8.0.0                      # Client Redis avec support asyncio (redis.asyncio)

# --- Authentification & Sécurité ---
passlib[bcrypt]==1.7.4            # Hachage bcrypt des mots de passe
# NOTE : bcrypt épinglé à 4.0.1 — passlib 1.7.4 (non maintenu) effectue un
#        auto-test interne avec un mot de passe de 73 octets que bcrypt >= 4.1
#        refuse désormais (ValueError). La protection DoS est assurée par le
#        max_length=72 dans les schémas Pydantic.
bcrypt==4.0.1
python-jose[cryptography]>=3.3.0  # Génération / décodage JWT (HS256, RS256)

# --- Utilitaires HTTP ---
anyio==4.13.0
h11==0.16.0
idna==3.17
httpx>=0.27.0                     # Client HTTP async (utilisé par les tests ASGI)

# --- Templates / Divers ---
Mako==1.3.12
MarkupSafe==3.0.3
click==8.4.1
colorama==0.4.6
greenlet==3.5.1

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0                 # Couverture de code
aiosqlite>=0.20.0                 # Driver SQLite async (base de test in-memory)
fakeredis>=2.0.0                  # Mock Redis in-memory (TTL + atomicité réels)
sortedcontainers>=2.0.0           # Dépendance de fakeredis
